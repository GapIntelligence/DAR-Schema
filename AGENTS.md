Keep commits small and focused.  
Use conventional commit messages.  
Format Markdown with Prettier.  
Run pytest for the small test suite.  
Run flake8 if you modify Python code.  
Document major changes in docs/.  
Keep filenames slug-case, functions snake_case, classes CapWords.  
Place tests next to code in \*\_test.py files.  
Summaries must cite changed files.  
PR descriptions must mention test results.  
Each PR should have a single purpose.  
Provide clear context when adding or updating links.  
AGENTS.md files use plain lines with two trailing spaces  —no bullet lists or headings.  
